/* eslint-disable no-console */

const trace = () => {
	console.trace(`%c trace`, 'color: #03A9F4; font-size: 12px; font-weight: bold;');
};

const loggerUtil00 = {
	trace,
};

export default loggerUtil00;
